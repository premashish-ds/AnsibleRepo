pipeline {
    agent {
        label 'masternodes'
    }
    stages {
        stage('Ansible Playbook Execution') {
            steps {
                sh 'ansible-playbook $WORKSPACE/jenkins_nginx_install.yml'
            }
        }
    }
}
